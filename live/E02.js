// Write a function named greet that takes a name as an argument and returns a string greeting that includes
// the name.

let name = 'kamar';
function greet(name){console.log('Hello '.concat(name));}

greet(name);