/*
Create a variable called password. If the password length is between 4 and 6, print the message
Password length is correct, otherwise print the message Password not valid.*/

let password = 'kam';

if (password.length >= 4 && password.length <= 6)
    console.log('password length is correct');
else
    console.log('password not valid');
